frontend:
    build:
      context: ./react
      dockerfile: Dockerfile.dev
    container_name: react-frontend
    ports:
      - "5173:5173"
    volumes:
      - ./react:/app
      - /app/node_modules
    command: ["npm", "run", "dev"]
    networks:
      - custom_net